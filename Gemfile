source 'https://rubygems.org'

ruby '2.3.1'

gem 'rails', '4.2.8'
gem 'rails-api'
gem 'rack-cors', :require => 'rack/cors'

group :development do
    gem 'spring'
    gem 'sqlite3'
    gem 'pry-rails'
end

group :production do
    gem 'pg'
end
